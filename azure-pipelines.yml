

# Build a Docker image
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

trigger:
- develop

pool:
 name: Default
 #name: Azure Pipelines
 
resources:
- repo: self



stages:
- stage: Build
  displayName: Build image
  jobs:
  - job: Build
    displayName: Build
    pool:
      name: Default

    steps:
    - script: |
              export PATH="$HOME/.local/bin:$PATH"
              . ~/.bashrc
              cd sopnode/ansible
              ansible-playbook  -i inventories/blueprint/ k8s-master.yaml --extra-vars "@params.blueprint.core.yaml"
      displayName: Ansible playbooks
    # - task: DockerInstaller@0
    #   inputs:
    #     dockerVersion: '17.09.0-ce'
    #   displayName: Install docker
    #     # Docker
    # - task: Docker@2
    #   inputs:
    #     containerRegistry: 'dockeregistry'
    #     repository: 'anassdahchour/blueprint'
    #     command: 'buildAndPush'
    #     Dockerfile: 'sopnode/ansible/Dockerfile'
    # - script: | 

    #           # git checkout tags/2nd-summer-school
    #           cd sopnode/ansible
    #   displayName: check dependencies
    # - script: |
    #           python3 -m pip -V
    #           echo $PATH

    #           ansible --version
    #           #ansible-playbook  -i inventories/blueprint/ k8s-master.yaml --extra-vars "@params.blueprint.yaml"
    #   displayName: Ansible